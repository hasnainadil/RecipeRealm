CREATE OR REPLACE PROCEDURE "ALL_TAGS"(STATUS OUT VARCHAR2,TAGS_CR OUT SYS_REFCURSOR) AS
BEGIN
OPEN TAGS_CR FOR 
SELECT *
FROM TAGS
ORDER BY NAME;
STATUS := 'SUCCESSFUL';
	-- routine body goes here, e.g.
	-- DBMS_OUTPUT.PUT_LINE('Navicat for Oracle');
	EXCEPTION
	WHEN OTHERS THEN
	STATUS := 'UNKNOW ERROR';
END;