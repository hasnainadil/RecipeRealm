CREATE OR REPLACE PROCEDURE "ALL_CATEGORIES"(STATUS OUT VARCHAR2,CATEGORIES_CR OUT SYS_REFCURSOR) AS
BEGIN
OPEN CATEGORIES_CR FOR 
SELECT *
FROM CATEGORIES
ORDER BY NAME;
STATUS := 'SUCCESSFUL';
	-- routine body goes here, e.g.
	-- DBMS_OUTPUT.PUT_LINE('Navicat for Oracle');
	EXCEPTION
	WHEN OTHERS THEN
	STATUS := 'UNKNOW ERROR';
END;